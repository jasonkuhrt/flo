# flo pr merge

```
Usage: flo pr merge

Merge the pull request for the current branch.

Requirements:
  - PR must be approved
  - All checks must pass
  - No merge conflicts

The merge will use the repository's default merge method.
```
